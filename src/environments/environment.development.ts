export const environment = {
  baseUrl: "http://localhost:4200",
  providerUrl: "http://localhost:3000",
  endpoints: {
    auth: {
      signin: "auth/signin",
      login: "auth/login"
    }
  }
};
